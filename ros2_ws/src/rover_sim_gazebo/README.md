# rover_sim_gazebo

Gazebo assets for integration testing:

- `worlds/pick_place_arena.sdf`: arena with walls, bins, platforms, blocks, and surrogate rover.
- `models/`: reusable SDF models for rover, bins, platforms, and colored blocks.

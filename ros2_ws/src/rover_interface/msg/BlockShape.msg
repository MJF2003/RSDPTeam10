# An enum defining the different block shapes as reported by the CV algorithm 
uint8 shape 

# enum definitions. These let us call the values in the following
# semantically useful way: shape = BlockShape.BRIDGE
uint8 BRIDGE = 0
uint8 CUBE = 1
uint8 CUBOID = 2
uint8 TRIANGULAR = 3
